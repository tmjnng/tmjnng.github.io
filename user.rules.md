# 个人规则 (user.rules.md)

## 工作环境
- **主要开发环境**：Windows
- **命令优先级**：优先使用Windows PowerShell/CMD命令
- **路径规范**：使用Windows风格路径（如 `C:\path\to\file`）
- **跨平台考虑**：代码应尽量保持跨平台兼容性，但命令和脚本优先考虑Windows环境

## 项目结构
- **根目录**：`C:\Users\NEWCON\Desktop\vbs\tool-workspace\tmjnng.github.io`
- **主要模块**：
  - `exam/` - JLPT日语刷题系统
  - `tool/` - 工具集合（加密、编码等）
  - `miniphotoedit/` - 图像编辑器
  - 其他网站相关文件

## 开发规范
- **代码风格**：使用ES6+语法，遵循JavaScript标准编码规范
- **命名规范**：函数和变量使用驼峰式命名
- **注释规范**：代码注释清晰明了，关键逻辑应有详细注释
- **文件命名**：使用小写字母和连字符，如 `aes-tools.js`

## 测试规范
- **测试文件位置**：所有测试文件必须放在对应模块的 `test/` 目录下
- **测试命名**：测试文件名应遵循 `test_*.html` 或 `*_test.js` 命名规范
- **测试执行**：优先使用浏览器端测试，确保功能在实际环境中正常工作

## 数据库操作
- **JLPT系统**：使用IndexedDB存储题目数据和用户进度
- **数据初始化**：确保数据库初始化完成后再进行操作
- **错误处理**：完善的错误处理机制，确保系统稳定性

## 工具使用
- **加密工具**：使用Web Crypto API进行加密操作
- **兼容性**：保持与其他网站工具的兼容性
- **用户界面**：提供友好的用户界面，确保操作简便直观
- **图像处理**：miniphotoedit模块中的图像处理需在客户端完成，不得上传至服务器

## 问题处理
- **错误定位**：使用浏览器控制台进行错误定位和调试
- **日志记录**：关键操作应有日志记录，便于排查问题
- **性能优化**：注意代码性能，避免不必要的重复操作
- **Canvas性能**：对于miniphotoedit模块，注意Canvas操作的性能，大图像处理时应提供进度提示

## 版本管理
- **文件管理**：不要在项目中生成多余的临时文件
- **代码备份**：重要修改前应备份相关文件
- **变更记录**：重要变更应记录在相关文档中